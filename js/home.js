"use strict";
{
    let home = {

    }, 

    controller: function (podcastService, $location) {
        let vm = this;
        vm.title = "";
        vm.
    }
    angular
        .module("app")
        .component("home", home);
}